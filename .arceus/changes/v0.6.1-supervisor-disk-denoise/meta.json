{
  "id": "v0.6.1-supervisor-disk-denoise",
  "title": "Supervisor (systemd service management) + disk-table de-noise — v0.6.1",
  "status": "completed",
  "smokeStatus": "2026-06-03 Rocky 234 smoke PASSED via scripts/smoke-toolbox-234.sh (v0.6.1 35ef1e7, deployed by operator, panel runs as root). S1 tools-present (3 features incl. services available), S2 fail2ban-absent 400, S3 NTP round-trip, S6 services: 180 .service units + protected guard verified LIVE (stop dinopanel/sshd + ssh.socket all -> SERVICE_PROTECTED, incl. the .service-only socket-bypass fix). Disk de-noise live: 27 fs rows, 15 overlay default-hidden, fstype present. S4 N/A (fail2ban not installed), S5 deferred (opt-in). See smoke-pass.md.",
  "createdAt": "2026-06-02",
  "targetVersion": "v0.6.1",
  "dependsOn": ["v0.6-toolbox"],
  "scopeNote": "v0.6.x supplement on top of the shipped toolbox. (1) Supervisor = systemd .service management as a 4th toolbox tab (list/status + start/stop/restart/enable/disable), NOT supervisord — both target distros are systemd and the codebase already shells systemctl (scheduler/nginx/firewalld). Tiered protected-units guard. (2) Disk-table de-noise: df -PTB1 adds an fstype column so the docker-overlay/pseudo rows can be filtered (default-hidden, UI toggle to show all). Stateless — no migration. Swap-write stays deferred (not in this cut).",
  "estimatedDevDays": "3-4",
  "phases": [
    { "id": "phase-1", "name": "Disk de-noise — df -PTB1 + fstype column + shared isRealFilesystem/PSEUDO_FSTYPES + web default-filter + 'show system filesystems' toggle + parser tests", "estimateDays": 0.5, "status": "completed", "note": "commit 099a7cc" },
    { "id": "phase-2", "name": "Supervisor backend — services-driver + SERVICES_DRIVER token + Unavailable fallback, status 'services' feature, routes, shared schemas + protected-units guard, guardrail tests", "estimateDays": 1.5, "status": "completed", "note": "commit 3ec61c3 — adversarial review 13->4 applied: HIGH .socket/.target bypass + MED alias bypass closed (.service-only guard + canonical-Id re-judge)" },
    { "id": "phase-3", "name": "Supervisor web UI — /toolbox 'services' tab + use-toolbox hooks + i18n (en + zh-TW)", "estimateDays": 1, "status": "completed", "note": "commit 748eb65 — protected buttons disabled via the shared serviceActionAllowed" },
    { "id": "phase-4", "name": "docs/toolbox.md (systemctl sudoers alias + Services section + protected-units note), release v0.6.1 (bump 0.6.0 -> 0.6.1, no migration), Rocky 234 smoke", "estimateDays": 1, "status": "completed", "note": "release 35ef1e7; deployed to Rocky 234 (root); smoke S1-S3 + S6 PASSED (S6 verified the protected guard live incl. ssh.socket -> SERVICE_PROTECTED); disk fstype de-noise confirmed live. See smoke-pass.md." }
  ],
  "directionDecisions": {
    "S1_supervisorKind": "systemd .service management (NOT supervisord). Both supported distros are systemd; codebase already uses systemctl (scheduler restart_service, nginx reload, firewalld driver). supervisord rejected: niche, needs separate install, high distro variance.",
    "S2_writeScope": "Full lifecycle (start/stop/restart/enable/disable) with TIERED protection: SELF (dinopanel.service) refuses ALL mutation; CRITICAL set (sshd/ssh, firewalld, ufw, NetworkManager/systemd-networkd, dbus/dbus-broker, systemd-logind, systemd-journald) refuses stop+disable but allows start/restart/enable; everything else fully allowed. Guard lives in shared (single source) so server enforces + web disables buttons.",
    "S3_diskDenoise": "df -PTB1 adds an fstype column (POSIX, never localized). Server returns ALL filesystems incl. fstype; the web default-hides pseudo/overlay rows via shared PSEUDO_FSTYPES + a 'show system filesystems' toggle. Filter by fstype (not path) so an unusual-but-real mount is never hidden.",
    "S4_persistence": "stateless — no drizzle table, no migration, live systemctl query only. No staged/confirm window needed because self-mutation is hard-refused outright.",
    "S5_privilege": "reuse TOOLBOX_REQUIRE_SUDO. Reads (list-units/list-unit-files) need no sudo; mutations run sudo -n systemctl <action> -- <unit>. Extend the DINOPANEL_TOOLBOX sudoers alias with systemctl start/stop/restart/enable/disable *; the protected-units denylist is enforced in CODE (sudoers can't encode 'all except self')."
  },
  "supersededDrafts": [],
  "previousChange": "v0.6-toolbox"
}
